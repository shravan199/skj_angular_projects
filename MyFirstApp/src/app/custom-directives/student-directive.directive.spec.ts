import { StudentDirectiveDirective } from './student-directive.directive';

describe('StudentDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new StudentDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
